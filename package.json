{
  "name": "landing_pages",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT",
    "dev": "json-server --watch db.json --port $PORT --delay 500"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
